<center><h1>Resultat de la recherche</h1></center>
<% if @product.empty? == true%>
		Aucun résultat
<% else %>
	<% if @product.empty? != true %>
		<% @product.each do |produit|%>
			<div>
				<h3> <%= link_to product.name, product %> </h3>
			</div>
		<% end %>
	<% end %>
<% end %>