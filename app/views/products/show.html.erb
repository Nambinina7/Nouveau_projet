<p id="notice"><%= notice %></p>
<p>Bienvenue sur la page d'ajout d'un avatar pour le Produit portant l'id :
<%=@product.id%></p>
<h3>Avatar actuel</h3>
<%if @product.avatar.attached?%>
<%= image_tag @product.avatar, alt: 'avatar' %>
<%else%>
<p>=== Il n'y a pas encore d'avatar lié à cet produit ===</p>
<%end%>
<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<h3>Changer d'avatar ?</h3>
<%= form_tag product_avatars_path(@product), multipart: true do %>
<%= file_field_tag :avatar %>
<%= submit_tag "mettre à jour" %>
<% end %>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
